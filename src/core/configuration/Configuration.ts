/*
 * =============================================================================
 * RTApps Enterprise
 * =============================================================================
 */

export class Configuration {

    public readonly darkMode = true;

    public readonly logging = true;

    public readonly autoSave = true;

    public readonly enableAI = true;

    public readonly enablePhysics = true;

    public readonly enableDICOM = true;

    public readonly enableSimulation = true;

}
